import DoaSection from '@/components/section/doaSection';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Do`a',
};

const Page = () => {
  return <DoaSection />;
};

export default Page;
