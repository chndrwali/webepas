import CrosswordSection from '@/components/section/crosswordSection';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'TTS',
};

const Page = () => {
  return <CrosswordSection />;
};

export default Page;
