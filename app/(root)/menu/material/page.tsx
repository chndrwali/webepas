import MaterialSection from '@/components/section/materialSection';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Materi',
};

const Page = () => {
  return <MaterialSection />;
};

export default Page;
