import MenuPadatSection from '@/components/section/menuPadatSection';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Padat',
};

const Page = () => {
  return <MenuPadatSection />;
};

export default Page;
