import MenuPropertySection from '@/components/section/menuPropertySection';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sifat Benda',
};

const Page = () => {
  return <MenuPropertySection />;
};

export default Page;
