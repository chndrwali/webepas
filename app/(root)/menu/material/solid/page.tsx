import MenuPerubahanSection from '@/components/section/menuPerubahanSection';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Perubahan Benda',
};

const Page = () => {
  return <MenuPerubahanSection />;
};

export default Page;
